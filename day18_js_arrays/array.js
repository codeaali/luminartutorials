var expenses = [10,20,30,40,50]

for(let val of expenses)
{
    console.log(val);
}

//default array printing.
console.log(expenses);

//old school method
for(let i=0;i<expenses.length;i++){
    console.log(expenses[i]);
}